spring:
  application:
    name: inf-sys

  datasource:
    password: 12345
    username: postgres
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/infsys

  jpa:
    hibernate:
      ddl-auto: create

  data:
    redis:
      host: redis
      port: 6379


server:
  port: 8081
